from setuptools import setup

setup(name='pwned_check', version='0.1',
      description='pwned_check package',
      url='http://github.com/truc/pwned_check',
      author='Pwned Check',
      author_email='test@example.com',
      license='MIT',
      packages=['pwned_check'],
      zip_safe=False)
